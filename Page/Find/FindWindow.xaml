<Window x:Class="ctxmgr.Page.Find.FindWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ctxmgr.Page.Find"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Title="Find" Height="200" Width="450">
    <Grid Margin="5,20,10,0" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- 查找内容 -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0" Margin="0,0,0,10">
            <Label Content="查找内容(N):" VerticalAlignment="Center"/>
            <TextBox x:Name="TxtKeyword" Width="200" Margin="5,0,0,0"/>
        </StackPanel>

        <!-- 按钮区 -->
        <StackPanel Grid.Row="0" Grid.Column="1" Grid.RowSpan="4"
                    Orientation="Vertical" HorizontalAlignment="Right">
            <Button x:Name="BtnFindNext" Content="查找下一个(F)" Width="100" Height="25" Margin="10,0,0,5"
                    Click="BtnFindNext_Click"/>
            <Button x:Name="BtnFindPrev" Content="查找上一个(P)" Width="100" Height="25" Margin="10,0,0,5"
                    Click="BtnFindPrev_Click"/>
            <Button Content="取消" Width="100" Height="25" Margin="10,20,0,0"
                    Click="BtnCancel_Click"/>
        </StackPanel>

        <!-- 选项 -->
        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Vertical">
            <CheckBox x:Name="ChkWholeWord" Content="全字匹配(W)" Margin="5,0,0,5"/>
            <CheckBox x:Name="ChkCaseSensitive" Content="区分大小写(C)" Margin="5,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
